import { MutifilterPipe } from './mutifilter.pipe';

describe('MutifilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MutifilterPipe();
    expect(pipe).toBeTruthy();
  });
});
